shader_type canvas_item;

// TODO
// basically just a shader that has ID / random lines that chanve with the
// SCREEN UV

//void vertex() {
	//// Called for every vertex the material is visible on.
//}
//
//uniform sampler2D noise_tex: repeat_enable;
//
//void fragment() {
	//vec2 uv = 2. * UV - 1.;
	//vec4 noise = texture(noise_tex, uv);
	//float mask = fract(5. * uv.x);
	//vec3 col = vec3(mask);
	//COLOR = vec4(col, 1.);
//}
